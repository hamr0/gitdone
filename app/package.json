{
  "name": "gitdone-receive",
  "version": "0.1.0",
  "private": true,
  "description": "GitDone receive pipeline — Phase 1 production code.",
  "main": "bin/receive.js",
  "scripts": {
    "dev": "scripts/dev-start.sh",
    "test": "node --test 'tests/unit/**/*.test.js' 'tests/integration/**/*.test.js'",
    "test:unit": "node --test 'tests/unit/**/*.test.js'",
    "test:integration": "node --test 'tests/integration/**/*.test.js'"
  },
  "dependencies": {
    "knowless": "^1.1.1",
    "mailauth": "^4.6.0",
    "mailparser": "^3.6.0",
    "simple-git": "^3.20.0"
  },
  "engines": {
    "node": ">=22.5"
  }
}
